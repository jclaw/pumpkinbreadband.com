# 1Password secret references for local dev.
# Run: npm run dev:op  (requires `op` CLI + active session)
# This file is safe to commit — it contains references, not secrets.
RESEND_API_KEY=op://Private/Resend/API key
